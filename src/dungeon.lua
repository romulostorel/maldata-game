-- Procedural dungeon generator: deterministic given a seed.
-- Outputs a tile grid (walls/floor), the entrance door, and the treasure position.
-- No rendering, no LÖVE calls — must run headless for tests.

local M = {}

return M
